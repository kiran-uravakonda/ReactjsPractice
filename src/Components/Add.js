function Add({fname,age,place}) {
    // console.log(props.data)
    return (
      <div>
        {/* <h1>{JSON.stringify(props.data)}</h1> */}
        <h1>{fname}</h1>
        <h1>{age}</h1>
        <h1>{place}</h1>
      </div>
    );
  }
  
  export default Add;
  